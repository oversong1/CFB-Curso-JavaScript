let num =1;
let num2 = 2;

/** A comparação funciona como um boolean
 * sendo que a primeira é se for verdadeiro
 * e a segunda se for falso
 */
res=(num > num2 ? "Passar" : "Não passa");

console.log(res);